var CameraPanConfig = {
    // Camera will smoothly pan when...
    // * Reinforcements appear
    enableReinforcementPan: true,

    // * Moving to the next enemy unit on enemy phase
    enableEnemyPan: true,

    // * The Phase Change animation plays
    enableTurnChangePan: true,

    // * The map cursor is moved (Recommended to keep on!)
    enableMouseMovementPan: true,

    // * Snapping to units on the map
    enableUnitSnapPan: true,

    // * Using the "Location Focus" event command
    enableLocationFocusPan: true,

    // Debug Enable
    enableDebug: false
};