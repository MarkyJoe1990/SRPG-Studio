var CameraPan = CameraPan || defineObject(BaseObject, {
    moveCameraPan: function() {
        return MoveResult.END;
    },

    setDestinationTileCenter: function() {
    },

    startCameraPan: function() {
    }
});

var CameraPanConfig = CameraPanConfig || {
    enableMouseMovementPan: false
}